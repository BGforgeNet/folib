/**
 * Astro + Starlight configuration for FOlib API documentation.
 *
 * API pages are generated by docs/generate.ts (run before dev/build).
 * Sidebar is autogenerated from the output directories.
 */
import starlight from '@astrojs/starlight'
import { defineConfig } from 'astro/config'

export default defineConfig({
  site: 'https://folib.bgforge.net',
  integrations: [
    starlight({
      title: 'FOlib',
      description: 'TypeScript bindings for Fallout 2 scripting',
      favicon: '/favicon.svg',
      customCss: ['./src/styles/signatures.css'],
      sidebar: [
        {
          label: 'Guides',
          items: [
            { label: 'Introduction', slug: 'index' },
            { label: 'Setup', slug: 'setup' },
          ],
        },
        {
          label: 'Core API',
          autogenerate: { directory: 'api/core' },
        },
        {
          label: 'sfall API',
          autogenerate: { directory: 'api/sfall' },
        },
        {
          label: 'Restoration Project',
          autogenerate: { directory: 'api/rp' },
        },
      ],
    }),
  ],
})
