/** AI chem-use mode values for AI_CAP.CHEM_USE. */
export enum ai_chem {
    clean = 0,
    stims_when_hurt_little = 1,
    stims_when_hurt_lots = 2,
    sometimes = 3,
    anytime = 4,
    always = 5,
}
