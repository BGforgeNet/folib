/** AI run-away mode values for AI_CAP.RUN_AWAY_MODE. */
export enum ai_run_away {
    none = 0,
    coward = 1,
    // Value 2 is unused in the engine
    finger_hurts = 3,
    bleeding = 4,
    not_feeling_good = 5,
    tourniquet = 6,
    never = 7,
}
